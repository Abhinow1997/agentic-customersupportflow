fastapi==0.115.5
uvicorn[standard]==0.32.1
python-multipart==0.0.12
boto3==1.35.99
pydantic==2.9.2
pydantic-settings==2.6.1
snowflake-connector-python==3.12.3
python-dotenv==1.0.1
cryptography>=43.0.1
# ── LangGraph ──────────────────────────────────────────────────────────────
# langchain-anthropic and anthropic removed — all LLM calls go through LiteLLM
langchain==0.3.25
langchain-core==0.3.58
langgraph==0.4.1

# ── LiteLLM — unified LLM gateway (OpenAI + any other provider) ───────────
litellm==1.57.4
openai==1.57.4
crewai==0.100.0

# ── RAG Pipeline (ChromaDB + Scrapling) ────────────────────────────────────
# Install in stages:
#   Stage 1 (core):  pip install chromadb tiktoken PyPDF2
#   Stage 2 (scraper): pip install scrapling
#   Stage 3 (browser): scrapling install   (downloads Playwright/Camoufox binaries)
#   Stage 4 (full):  pip install "scrapling[all]"  (only if you need StealthyFetcher)

chromadb
tiktoken
PyPDF2
scrapling
